// import java.util.ArrayList;

public class BasicsTest {
    public static void main(String[] args) {
        Basics b = new Basics();

        // b.printRange();
        // b.printOddRange();
        // b. printSum();

        // int[] arr = {1,3,5,7,9,13};
        // b.iteratingThroughAnArray(arr);

        // int[] arr = { -3,-5,-7};
        // b.findMax(arr);

        // int[] arr = {2,10,3};
        // b.getAverage(arr);

        // b.arrayWithOddNumbers();

        // int[] arr = { 1,3,5,7 };
        // int y = 3;
        // b.greaterThanY(arr, y);

        // int[] arr = { 1,5,10,-2 };
        // b.squareTheValues(arr);

        // int[] arr = { 1,5,10,-2 };
        // b.eliminateNegativeNumbers(arr);

        // int[] arr = { 1,5,10,-2 };
        // b.maxMinAverage(arr);

        int[] arr = { 1, 5, 10, 7, -2 };
        b.shiftValuesInArray(arr);
    }
}
